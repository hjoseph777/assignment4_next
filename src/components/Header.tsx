// Header.tsx - Defines the header component for the application.
import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1>My Website</h1>
        </header>
    );
};

export default Header;
